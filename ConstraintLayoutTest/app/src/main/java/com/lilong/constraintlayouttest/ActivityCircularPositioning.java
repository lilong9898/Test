package com.lilong.constraintlayouttest;

import android.app.Activity;
import android.os.Bundle;

/** constraint layout中的circular positioning*/
public class ActivityCircularPositioning extends Activity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_circular_positioning);
        setTitle("CircularPositioning");
    }
}
